%tr{:align => 'right', :data_user_id => user.id}
  %td= user.id
  %td= user.first_name
  %td= user.last_name
  %td= user.email
  %td= user.user_country + ", " + user.user_state + ", " + user.user_city + ", " + user.user_address
  %td
    %span.deleteUser Delete